# Copy to ~/.ytplay/.env and edit.
# Do NOT commit secrets.

OPENAI_API_KEY=change_me

# Optional
OPENAI_MODEL=gpt-5-mini
YTP_PORT=17845
YTP_MAX_TRACKS=25
YTP_CACHE_TTL_HOURS=72

# Where daemon stores state (db, mpv socket)
YTP_STATE_DIR=~/.ytplay
